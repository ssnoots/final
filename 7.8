def IndexOfSmallestElement(lst):
    index = 0
    smallest = lst[0]
    for i in range(1, len(lst)):
        if smallest > lst[i]:
            index = i
            smallest = lst[i]
    return index
s = input()
l = s.split()
print(IndexOfSmallestElement(l))
